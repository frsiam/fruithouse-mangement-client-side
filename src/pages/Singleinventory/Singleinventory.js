import React from 'react';

const Singleinventory = ({ inventory }) => {
    return (
        <div>
            <h1>single inventory: {inventory.name}</h1>
        </div>
    );
};

export default Singleinventory;